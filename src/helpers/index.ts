/**
 * Generates a title by appending a suffix to the provided title.
 * @param title - The base title.
 * @returns The generated title.
 */
export const generateTitle = (title: string) => `${title} | Adam Shappy`;
