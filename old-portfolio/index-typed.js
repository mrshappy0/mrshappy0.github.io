window.addEventListener("DOMContentLoaded", (event) => {
    console.log("DOM fully loaded and parsed");
    let indexName = new Typed("#indexName", {
      strings: [`<strong>Adam Shappy</strong>`],
      typeSpeed: 60,
      fadeOut: true,
      showCursor: true,
    });

  });